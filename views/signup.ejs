<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width,initial-scale=1.0" />
    <meta name="csrf-token" content= "<%= csrfToken %>">
    <link href="./css/style.css" rel="stylesheet" />
    <title>
        <%= title %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="./css/style.css" rel="stylesheet" />
  </head>
  <body style="background-color: rgba(223, 223, 223, 0.988);">
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
          <%- include("header.ejs") %> 
          <p class="py-4"> <a href="/" class="bg-red-600 text-white px-5 py-2 rounded font-medium mr-2 mb-2 border-green-300" >Let's Go Back To Home Page</a> </p>
          <h6 class="py-4">Sign up If You are New User</h6>
          <form action="/users" method="post">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
           <div class="py-3">
           <label for="firstName">FIRST NAME:</label>
           <input type="text" name="firstName" class="border border-green-400 text-green-800 rounded p-2 w-full">
        </div>
        <div class="py-3">
           <label for="lastName">LAST NAME:</label>
           <input type="text" name="lastName" class="border border-gray-400 text-gray-800 rounded p-2 w-full">
        </div>
        <div class="py-3">
           <label for="email">Email iD:</label>
           <input type="email" name="email" class="border border-gray-400 text-gray-800 rounded p-2 w-full">
        </div>  
        <div class="py-3"> 
           <label for="password">Type Your Password:</label>
           <input type="password" name="password" class="border border-gray-400 text-gray-800 rounded p-2 w-full">
        </div>  
        <div class="py-3"> 
           <button type="submit" class="text-white bg-red-600 hover:bg-blue-800 font-medium rounded px-5 py-2">Sign up Now</button>        
        </div>  
        </form>
        <% if(messages && messages.error && messages.error.length > 0)  { %>
         <% for(var i=0; i<messages.error.length; i++) { %>
          <p class="bg-red-300 my-3 list-none rounded px-4 py-2"><%= messages.error[i] %></p>
         <% } %>
       <% } %>
        </div>
    </div>
  </body>
</html>
